services:
  - type: web
    name: haru-one-line-api
    runtime: node
    region: singapore
    plan: free
    rootDir: server
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: haru-one-line-db
          property: connectionString
      - key: FIREBASE_PROJECT_ID
        sync: false
      - key: CORS_ORIGINS
        sync: false
databases:
  - name: haru-one-line-db
    region: singapore
    plan: free
    databaseName: haru
    user: haru
